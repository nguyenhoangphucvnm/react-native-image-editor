/**
 * HomepageFeatures is no longer rendered on the home page —
 * the landing page (src/pages/index.tsx) renders its own inline feature cards.
 *
 * This file is kept as a no-op export so any residual imports don't break
 * the build during the migration.
 */
export default function HomepageFeatures() {
  return null;
}
